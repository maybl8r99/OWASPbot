{
  "name": "owasp-scanner",
  "version": "1.0.0",
  "description": "OWASP Security Scanner - SAST and DAST",
  "scripts": {
    "dast": "playwright test --project=dast",
    "dast:headed": "playwright test --project=dast --headed",
    "dast:report": "playwright show-report reports/dast-report",
    "install:playwright": "playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.3.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
