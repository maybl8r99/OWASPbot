# =============================================================================
# Security Scanning Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Target Application Settings
# -----------------------------------------------------------------------------
TARGET_ENDPOINT=https://your-target-website.com
TARGET_NAME=MyApplication

# -----------------------------------------------------------------------------
# Repository Settings (for SAST)
# Multiple repos are cloned into ./source_code/ subfolders
# Structure: ./source_code/repo-frontend/, ./source_code/repo-api/, etc.
# -----------------------------------------------------------------------------
SOURCE_CODE_PATH=./source_code

# Branch to checkout
REPO_BRANCH=main

# Azure DevOps Repositories
# Format: https://dev.azure.com/{org}/{project}/_git/{repo}
# Authentication: Script will prompt for credentials when run
#   - Username: Can be anything (e.g., "user" or your email)
#   - Password: Your Personal Access Token (PAT) with "Code > Read" scope
#   - Leave both blank for public repos
REPO_FRONTEND=https://dev.azure.com/your-org/your-project/_git/frontend
REPO_API=https://dev.azure.com/your-org/your-project/_git/api
REPO_SHARED=https://dev.azure.com/your-org/your-project/_git/shared-lib

# Alternative: Space-separated URLs
# REPO_URLS=https://dev.azure.com/org/proj/_git/repo1 https://dev.azure.com/org/proj/_git/repo2

# -----------------------------------------------------------------------------
# Playwright DAST Settings
# -----------------------------------------------------------------------------
HEADLESS=true

# Authentication
# Set to true if testing a public app without authentication
SKIP_AUTH=false

# Scan settings
SCAN_DEPTH=2
MAX_PAGES=50
TIMEOUT_MS=30000

# -----------------------------------------------------------------------------
# SAST Tool Configuration
# Recommended: SonarQube Community (full-featured) or Semgrep (lightweight)
# -----------------------------------------------------------------------------
SAST_TOOL=sonarqube

# SonarQube Settings
SONARQUBE_URL=http://localhost:9000
SONARQUBE_TOKEN=
SONAR_PROJECT_KEY=my-project
SONAR_SOURCES=./source_code
# Exclude common non-source directories
SONAR_EXCLUSIONS=**/node_modules/**,**/dist/**,**/build/**,**/.git/**,**/vendor/**

# Alternative: Semgrep Settings (if SAST_TOOL=semgrep)
SEMGREP_CONFIG=auto
SEMGREP_RULES=p/ci,p/security-audit,p/owasp-top-ten
SEMGREP_PATH=./source_code

# -----------------------------------------------------------------------------
# OWASP ZAP Settings
# -----------------------------------------------------------------------------
ZAP_API_URL=http://localhost:8080
ZAP_FORMAT=html
# ZAP_OPENAPI_URL=http://localhost:3000/api-docs  # For API scanning

# -----------------------------------------------------------------------------
# Docker Compose Settings
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=owasp-scanner
